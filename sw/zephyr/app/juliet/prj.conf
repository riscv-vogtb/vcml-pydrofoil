# Board defconfig already carries the VP quirks (no tick source, no PMP).
# The malloc testcase categories will need a libc heap arena here.

# The buffer testcases put several 100-element arrays in one frame.
CONFIG_MAIN_STACK_SIZE=4096
